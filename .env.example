# SQLite database file used by Prisma. Relative to the prisma/ folder.
DATABASE_URL="file:./dev.db"

# Secret used to HMAC-sign session cookies. Any long random string works.
# Generate one locally with: openssl rand -hex 32
SESSION_SECRET="change-this-to-a-long-random-string-before-deploying"
